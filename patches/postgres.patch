diff --git a/BUILD.bazel b/BUILD.bazel
index 8f58f78..0c320cb 100644
--- a/BUILD.bazel
+++ b/BUILD.bazel
@@ -1300,13 +1300,13 @@ cc_library(
     }),
     deps = select({
         ":ssl_boringssl": [
-            "@boringssl//:crypto",
-            "@boringssl//:ssl",
+            "@ssl_lib//:crypto",
+            "@ssl_lib//:ssl",
         ],
         ":ssl_none": [],
         ":ssl_openssl": [
-            "@openssl//:crypto",
-            "@openssl//:ssl",
+            "@ssl_lib//:crypto",
+            "@ssl_lib//:ssl",
         ],
     }) + select({
         # Curl is only linked where the OAuth flow is supported (libcurl +
diff --git a/MODULE.bazel b/MODULE.bazel
index 45ce407..7c346a0 100644
--- a/MODULE.bazel
+++ b/MODULE.bazel
@@ -10,8 +10,7 @@ bazel_dep(name = "rules_cc", version = "0.2.4")
 bazel_dep(name = "rules_cc_autoconf", version = "0.9.0")
 bazel_dep(name = "platforms", version = "1.0.0")
 bazel_dep(name = "bazel_skylib", version = "1.8.1")
-bazel_dep(name = "boringssl", version = "0.20241024.0")
-bazel_dep(name = "openssl", version = "3.5.5.bcr.4")
+bazel_dep(name = "ssl_lib", version = "1.0.0")
 bazel_dep(name = "curl", version = "8.12.0.bcr.1")
 bazel_dep(name = "lz4", version = "1.9.4.bcr.2")
 bazel_dep(name = "readline", version = "8.3.bcr.1")
